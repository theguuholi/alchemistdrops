# Articles (Markdown)

Blog posts are rendered with **GFM-style pipe tables** enabled. Each table row must be on its **own line** (header, separator `|---|---|`, and body rows). If you paste from Google Docs or Word and rows collapse into one line, fix line breaks in the editor before publishing—otherwise the table may render as a plain paragraph.
